# Core API
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.4
pydantic-settings==2.3.0
python-dotenv==1.0.1
python-multipart==0.0.9
aiofiles==23.2.1

# Database
sqlalchemy==2.0.30
alembic==1.13.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
aiosqlite==0.20.0

# Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# HTTP / WebSocket
httpx==0.27.0
websockets==12.0

# AI / LLM
openai==1.30.0
langchain==0.2.5
langchain-openai==0.1.8
langchain-pinecone==0.1.1
pinecone-client==3.2.2

# Billing / Cloud
stripe==9.12.0
boto3==1.34.0

# Task queue / Rate limit
redis==5.0.4
celery[redis]==5.4.0
slowapi==0.1.9

# Audio / ML (core — always installed)
numpy==1.26.4
soundfile==0.12.1
pypdf==4.2.0

# This file is the source of truth for Docker builds.
# To regenerate from pyproject.toml: poetry export -f requirements.txt --without dev --without-hashes -o requirements.txt
#
# Optional ML fallbacks — install manually for local/non-Docker dev:
#   pip install faster-whisper fasttext-wheel librosa torch transformers sentencepiece sacremoses indic-nlp-library flower
# These are NOT included in Docker to keep the image lean.
# The app degrades gracefully when they are absent (Sarvam API is the primary provider).
